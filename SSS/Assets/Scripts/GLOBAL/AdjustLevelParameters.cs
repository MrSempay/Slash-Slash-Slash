using System.Collections.Generic;
using UnityEngine;

public static class AdjustLevelParameters
{

    /*    public static readonly Dictionary<string, Dictionary<string, object>> levelParameters =
            new Dictionary<string, Dictionary<string, object>>()
            {
            {
                C.DK.Level1, new Dictionary<string, object>()
                {
                    { C.DK.targetPointsForEnemy, new Dictionary<Transform, int>() },
                }
            }, }; 

        // получаем параметр из словаря по названию юнита и параметра
        public static object GetParameter(string levelName, string parameterName)
        {
            if (unitParameters.ContainsKey(levelName) && unitParameters[levelName].ContainsKey(parameterName))
            {
                return unitParameters[levelName][parameterName];
            }
            return null;
        }

        // получаем весь словарь для отдельного юнита по его имени
        public static object GetSetupOfUnit(string levelName)
        {
            if (unitParameters.ContainsKey(levelName))
            {
                return unitParameters[levelName];
            }
            return null;
        }
    */
}
